python3 -m venv nmyenv --system-site-packages
source myenv/bin/activate #wejście w ścieżkę środowiska

pip install numpy
pip install opencv-python
pip install picamera2
pip install pillow
pip install gpiozero

